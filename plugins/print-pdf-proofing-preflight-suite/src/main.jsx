/* 印刷・PDF・校正プリフライトスイート */
(function () {
  var profile = {
    name: "印刷・PDF・校正プリフライトスイート",
    host: "Acrobat",
    repository: "print-pdf-proofing-preflight-suite"
  };
  $.writeln(profile.name + " loaded for " + profile.host);
}());
